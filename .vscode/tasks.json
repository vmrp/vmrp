{
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make",
            "args": [
                // "DEBUG=1",
                // "${file}",
                // "-o",
                // "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                // "cwd": "/usr/bin"
            }
        },
        {
            "type": "shell",
            "label": "win build current file",
            "command": "gcc",
            "args": [
                "-g",
                "\"${file}\"",
                "-o",
                "\"${fileDirname}/${fileBasenameNoExtension}.exe\""
            ],
            "options": {
                // "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}